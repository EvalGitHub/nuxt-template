{
  "name": "nuxt-app",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "\t#打包 \n \t nuxt build ",
    "predev": "rm -rf .output && npm run build",
    "dev": "\t#启动 \n \t nuxt dev",
    "postdev": "echo '开发环境启动成功'",
    "generate": "nuxt generate",
    "preview": "#ssr \n npm run build && nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^4.1.1",
    "vue": "^3.5.21",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@element-plus/nuxt": "^1.1.4",
    "sass-embedded": "^1.92.1"
  }
}
